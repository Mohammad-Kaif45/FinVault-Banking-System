server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      # --- CORS CONFIGURATION ---
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins: "http://localhost:5173"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
            allowedHeaders: "*"
      # --- END CORS ---

      routes:
        - id: user-service
          uri: lb://USER-SERVICE
          predicates:
            - Path=/users/**
          metadata:
            response-timeout: 10000  # <--- Changed to 10 seconds
            connect-timeout: 10000   # <--- Changed to 10 seconds

        - id: account-service
          uri: lb://ACCOUNT-SERVICE
          predicates:
            - Path=/accounts/**
          metadata:
            response-timeout: 10000  # <--- Changed to 10 seconds
            connect-timeout: 10000   # <--- Changed to 10 seconds

        - id: transaction-service
          uri: lb://TRANSACTION-SERVICE
          predicates:
            - Path=/transactions/**
          metadata:
            response-timeout: 10000  # <--- Changed to 10 seconds
            connect-timeout: 10000   # <--- Changed to 10 seconds

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true